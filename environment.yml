name: compare_vol
channels:
  - pytorch
  - nvidia
  - torch
  - conda-forge
  - omnia
  - salilab
  - numpy
  - defaults
dependencies:
  - matplotlib 
  - pip
  - pytorch 
  - tqdm 
  - natsort 

  - pip:
      - mrcfile==1.5.0
      - starfile
      - pytest-warnings
prefix: cryosvd
